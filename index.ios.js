/**
 * Sample React Native App
 * https://github.com/facebook/react-native
 */

import React, {
  AppRegistry
} from 'react-native';

var IntegrationTestApp = require('./IntegrationTests/IntegrationTestsApp');

AppRegistry.registerComponent('RNIntegrationTestExample', () => IntegrationTestApp);
